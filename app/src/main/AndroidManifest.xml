<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.finalmufixapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/mufix_logo"
        android:roundIcon="@drawable/mufix_logo"
        android:supportsRtl="true"
        tools:replace="android:icon">
        <activity android:name=".Activites.User"
            android:theme="@style/NoActionBarTheme" ></activity>
        <activity
            android:name=".Activites.Maps"
            android:theme="@style/FullScreen" />
        <activity
            android:name=".Activites.Tables"
            android:theme="@style/FullScreen" />
        <activity
            android:name=".Activites.StartActivity"
            android:theme="@style/FullScreen" />
        <activity
            android:name=".Activites.Image"
            android:theme="@style/NoActionBarTheme" />
        <activity
            android:name=".Activites.Splash"
            android:theme="@style/FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activites.About"
            android:theme="@style/NoActionBarTheme" />
        <activity
            android:name=".Activites.Edit"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Activites.Comments"
            android:label="Comments"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Activites.Home"
            android:theme="@style/FullScreen" />
        <activity
            android:name=".Activites.MainActivity"
            android:theme="@style/FullScreen" />
    </application>

</manifest>